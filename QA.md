# 검증 기록

## 0.6.2 (저장 형식)

- 단위·DOM 테스트 **25개 통과**. 추가된 테스트는 다음을 확인합니다.
  - 모든 단계(예상, 추천, 드래프트 매 지명, 인터뷰, 1–5시즌, 평가, 시즌 후 인터뷰 재방문)에서 새 저장 → 불러오기 결과가 원본과 완전히 같은지
  - 다른 `sim` 저장과 잘못된 입력을 거부하는지
  - v0.6.0 저장(감싼 형식과 게임 객체 그대로 둘 다)을 불러올 수 있는지
  - 다른 버전 자동 저장을 따로 보관하고 덮어쓰지 않는지
- 골든 마스터 일치(`SIM_VERSION 0.6`). 상수 하나를 바꿔 보는 방식으로 안전장치를 확인했습니다. 버전을 그대로 두면 테스트 실패와 `--write` 거부가 일어나고, 버전을 올리면 이전 저장이 거부됐습니다.
- Chromium 4개 화면 크기 완주. 자동 저장이 2KB 미만인지 매 단계 확인하고, 내보낸 5년 진행 파일 355–369바이트, 가져오기 후 동일함을 확인했습니다.
- 릴리스 확인 테스트: v0.6.0 자동 저장이 있는 브라우저에서 열면 게임이 이어지고 새 형식으로 바뀌는지, 예제 파일 가져오기가 되는지 확인했습니다.

## 0.6.1 (코드 정리·화면 개편)

- 단위·DOM 테스트 **21개 통과**(`tests/v06-core-results.txt`). 저장 관련 두 테스트는 새 복원 방식에 맞춰 고쳤습니다.
  - 파생 데이터 변조는 무시하고 정직한 값으로 복구하는지 확인합니다.
  - 입력 변조(버전, CPU 지명, 불법 지명, 시즌 수, 기자회견 답, 시드, 커서)는 거부하는지 확인합니다.
- **골든 마스터 24개 해시 일치**: 선수 풀 3개, 5년 완주 게임 6개를 리팩터링 전 원본 코드와 비교했습니다. 숫자·지명 결과는 모두 같고, 문장만 달라졌습니다.
- **600회 밸런스 실험 결과가 0.6.0과 바이트 단위로 동일**(`tests/v06-balance-results.json`).
- 동봉한 0.6.0 데모 저장이 새 복원 방식으로 바이트 단위까지 똑같이 복원됩니다.
- 실제 Chromium, 오프라인 `file://`, 4개 화면 크기(1440×1000, 390×844, 820×1180, 320×740)에서 완주했습니다(`tests/v06-browser-results.txt`).
  - 확인 항목: 5시즌 진행, JSON 내보내기·초기화·가져오기 동일성, PNG 저장.
  - 페이지 오류와 가로 넘침은 없었습니다.
- 모든 화면·모달에서 `undefined`/`NaN` 노출이 없는지 jsdom으로 따로 확인했습니다.
- 한계: iOS Safari·Android 실기기는 여전히 검증하지 않았습니다.

---

## 0.6.0

## 완료한 검증

- 코어·DOM 자동 테스트 **21개 통과**.
- 10구단 × 지역 ON/OFF × 3난이도 = **60개 조합**, 각 조합의 지명·5시즌·연도별 JSON 재생 검증.
- 전 구단 AI 밸런스 실험 **600회**, 난이도별 200개 시드, 지역 ON/OFF 각 100회. **45,000명·225,000 선수-시즌**.
- 실제 Linux Chromium 오프라인 `file://` 완주 **4개 화면 크기**: 1440×1000, 390×844, 820×1180, 320×740.
- 브라우저에서 추천 3명·추천 단계 재로드·능력치 4항목·유형 필터·이지 전용 선호 구단 표시·전체 구단 기록·퓨처스·선수 프로필·5시즌 진행·매년 재로드·최종 평가·JSON 내보내기/초기화/가져오기 동일성·PNG 다운로드 확인.
- 페이지/콘솔 오류 없음, 문서 전체 가로 넘침 없음. 모바일 표는 의도적인 내부 가로 스크롤이며 키보드로도 확인.
- 모바일 진행 단계의 부분 가림을 2행 배치로 수정하고 320/390px에서 모두 들어오는지 자동 검증.
- 도루 음수/음수 0 경계값 수정. 투수 삼진은 아웃 수 이하, 선발승은 5이닝 이상 선발 투구를 조건으로 생성.
- 타율·OPS·ERA 누적 재계산, 수상 당시 소속, 원지명/현소속 분리, 과거 뉴스·첫해 약속·선수 원본 불변성 검증.
- AI·기사·추천의 숨은 기량/잠재력 접근 차단 검증. 세부 툴을 바꾸는 통제 실험으로 관련 기록의 방향성 검증.
- 방출은 3~4년 차 종료, 만 23세 이상·2년 연속 1군 무기록·공개 기량 40 미만 조건을 실험 중 확인.

## 최종 노말 200회 요약

아래는 가상 게임의 튜닝 결과이지 실제 KBO 통계가 아닙니다.

- 전년도 안착 상태 다음 해 유지: **87.39%**, 17,117개 전이.
- 건강 제한 표시 없고 같은 팀인 전이의 안착 유지: **88.32%**, 15,201개 전이.
- 안착에서 다음 해 퓨처스 전념: **0.11%**. 장기 재활은 별도 상태입니다.
- 전국 1라운드 첫해 1군 경험 **89.35%**, 안착 **36.10%**.
- 3~5년 차 안착 선발 평균 **161.36이닝·12.24승**.
- 3~5년 차 안착 야수 평균 **127.81경기·12.71홈런**.
- 생성된 선수에서 최대 **23승**, 최대 **47홈런**. 두 자릿수 선발승 6,025 선수-시즌, 20홈런 이상 2,340 선수-시즌.
- 후보 200명당 FV 60 이상 평균 **5.06명**. 전국 3라운드 평균 FV **48.26**.
- 5년 후 지명 당시 공개 FV 이상 평가를 받은 비율 **73.90%**.

V0.5와 V0.6은 후보 생성과 척도 정의가 다릅니다. 같은 선수를 통제한 인과 비교가 아니며, 기존 100점 척도의 ‘5점 이내’와 V0.6 20–80 척도의 ‘5점 이내’를 같은 의미로 비교해서는 안 됩니다. 방출 선수는 마지막 저장 기량을 사용합니다.

## 남은 검증 / 한계

- 실제 Safari/iOS 및 Android 실기기 실행은 검증하지 않았습니다. 모바일 결과는 Linux Chromium의 화면 크기 테스트입니다.
- 최종 보정 후 모바일 레이아웃은 자동 검사했지만 모든 화면에 대한 사람의 재시각 검토를 완료한 것은 아닙니다. 초기 스크린샷 검토에서 큰 레이아웃 붕괴는 없었습니다.
- 장기 플레이 재미, 너무 높은/낮은 특정 포지션 성적, 팀장 성향별 체감은 사용자 플레이 피드백으로 추가 튜닝이 필요합니다.
- 실제 KBO 규정·성적과의 통계적 보정, 선수단 전체 경기 단위 대조는 하지 않았습니다.
- 능력치·예측은 로컬 JavaScript 게임 데이터입니다. 저장 파일은 편집 탐지를 위해 재생 검증하지만 암호화된 비밀 데이터나 부정행위 방지 서버가 아닙니다.

상세 원시 집계와 테스트 실행 로그는 `tests/v06-*-results.*`에 포함되어 있습니다.
